# Figures

Save the PNGs generated by the notebooks into this folder so the main README can display them:

From `Network_Intrusion_Detection.ipynb`:
- `traffic_distribution.png`
- `model_comparison.png`
- `confusion_matrix.png`
- `feature_importance.png`

From `Cross_Day_Generalization.ipynb`:
- `exp1_per_attack_recall.png`
- `generalization_gap.png`

On Google Colab, download them with:
```python
from google.colab import files
for f in ['generalization_gap.png', 'exp1_per_attack_recall.png',
          'model_comparison.png', 'confusion_matrix.png',
          'feature_importance.png', 'traffic_distribution.png']:
    files.download(f)
```
